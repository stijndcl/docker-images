orbs:
  docker: circleci/docker@0.5.13

version: 2.1

workflows:
  lint:
    jobs:
      - docker/hadolint:
          name: anaconda
          dockerfiles: dodona-anaconda3.dockerfile
      - docker/hadolint:
          name: bash
          dockerfiles: dodona-bash.dockerfile
      - docker/hadolint:
          name: biopythia
          dockerfiles: dodona-biopythia.dockerfile
      - docker/hadolint:
          name: csharp
          dockerfiles: dodona-csharp.dockerfile
      - docker/hadolint:
          name: haskell
          dockerfiles: dodona-haskell.dockerfile
      - docker/hadolint:
          name: java
          dockerfiles: dodona-java.dockerfile
      - docker/hadolint:
          name: nodejs
          dockerfiles: dodona-nodejs.dockerfile
      - docker/hadolint:
          name: prolog
          dockerfiles: dodona-prolog.dockerfile
      - docker/hadolint:
          name: r
          dockerfiles: dodona-r.dockerfile
      - docker/hadolint:
          name: scratch
          dockerfiles: dodona-scratch.dockerfile
